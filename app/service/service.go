package service

/* type serviceInterface interface {
	SignIn(username, password string) (token string, err error)
	SignUp(username, password, email string) (token string, err error)
	GetAllUsers() (users []models.User, err error)
	Profile(token string) (user models.User, err error)
	DeleteUser(token string) (user models.User, err error)
	UpdateUser(token string) (user models.User, err error)
} */

// type service struct{}

/* func (service) SignIn(username, password string) (token string, err error) {
	return
} */

/* func (service) SignUp(username, password, email string) (token string, err error) {
	return
} */

/* func (service) GetAllUsers() (users []models.User, err error) {
	return
} */

/* func (service) Profile(token string) (user models.User, err error) {
	return
} */

/* func (service) DeleteUser(token string) (user models.User, err error) {
	return
} */

/* func (service) UpdateUser(token string) (user models.User, err error) {
	return
} */
